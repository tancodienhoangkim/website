<div class="d-flex justify-content-between align-items-center mb-3">
  <h2>Dự án</h2>
  <a href="/admin/projects/create" class="btn btn-primary"><i class="fas fa-plus"></i> Thêm mới</a>
</div>
<table class="table table-bordered bg-white">
  <thead class="thead-light">
    <tr><th>Ảnh</th><th>Tên</th><th>Danh mục</th><th>Phong cách</th><th>Nổi bật</th><th>Trạng thái</th><th width="150">Hành động</th></tr>
  </thead>
  <tbody>
    <% projects.forEach(p => { %>
    <tr>
      <td><% if (p.thumbnail) { %><img src="<%= p.thumbnail %>" alt=""><% } else { %>—<% } %></td>
      <td><%= p.title %></td>
      <td><%= p.category ? p.category.name : '—' %></td>
      <td><%= p.style || '—' %></td>
      <td><%= p.featured ? '⭐' : '' %></td>
      <td><span class="badge badge-<%= p.status === 'published' ? 'success' : 'secondary' %>"><%= p.status %></span></td>
      <td>
        <a href="/admin/projects/<%= p._id %>/edit" class="btn btn-sm btn-warning"><i class="fas fa-edit"></i></a>
        <form action="/admin/projects/<%= p._id %>?_method=DELETE" method="POST" class="d-inline" onsubmit="return confirm('Xóa?')">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <button class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
        </form>
      </td>
    </tr>
    <% }) %>
    <% if (!projects.length) { %><tr><td colspan="7" class="text-center text-muted">Chưa có dự án</td></tr><% } %>
  </tbody>
</table>
